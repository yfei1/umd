#include <stdio.h>
#include <assert.h>
#include "header07.h"

/* this represents one test of the functions in pgm7a.c */

/* this test has syntax errors, so it won't compile! */

int main(void) {
  assert(product(2, 16) == 32

  printf("The student code in studentcode07.c works on its third test!\n");

  return 0;
}
